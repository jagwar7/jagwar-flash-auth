version: '3.8'

services:
  backend:
    build: 
      context: ./server
    container_name: express-backend
    ports:
      - "5000:5900"  # Map localhost:5000 → container:5900
    environment:
      - NODE_ENV=development
    volumes:
      - ./server:/app
      - /app/node_modules